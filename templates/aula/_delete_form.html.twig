<form method="post" action="{{ path('app_aula_delete', {'id': aula.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aula.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
